from __future__ import annotations
from typing import List, Tuple, Dict
import numpy as np

def head_alignment_map(heads_A:List[Tuple[int,int]], heads_B:List[Tuple[int,int]]) -> Dict:
    """Placeholder: compute a trivial bipartite map."""
    return {(0,0):(0,0)}
